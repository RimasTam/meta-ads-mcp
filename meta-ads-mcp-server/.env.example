# Meta Ads MCP Server Environment Variables
# Copy this file to .env for local development
# For Railway deployment, set these variables in the Railway dashboard

# ============================================================================
# META DEVELOPER APP CREDENTIALS (Required for self-hosted authentication)
# ============================================================================
# Get these from: https://developers.facebook.com/apps/
# 1. Create a new app or use existing app
# 2. Go to Settings > Basic
# 3. Copy App ID and App Secret

META_APP_ID=your_meta_app_id_here
META_APP_SECRET=your_meta_app_secret_here

# ============================================================================
# ALTERNATIVE: PIPEBOARD AUTHENTICATION (Optional - if using Pipeboard.co)
# ============================================================================
# If you prefer simplified authentication via Pipeboard service
# Get from: https://pipeboard.co/api-tokens
# NOTE: You mentioned wanting self-hosted, so you'll likely use META_APP_ID/SECRET instead

# PIPEBOARD_API_TOKEN=your_pipeboard_token_here

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# Port for the HTTP server (Railway sets this automatically)
PORT=8080

# ============================================================================
# RAILWAY DEPLOYMENT NOTES
# ============================================================================
# When deploying to Railway:
# 1. DO NOT commit this file with real values
# 2. Set environment variables in Railway dashboard:
#    - Go to your project > Variables tab
#    - Add META_APP_ID and META_APP_SECRET
#    - PORT is automatically set by Railway (no need to add)
# 3. The server will run with:
#    python -m meta_ads_mcp --transport streamable-http --host 0.0.0.0 --port $PORT
# ============================================================================

# ============================================================================
# HOW TO GET META DEVELOPER CREDENTIALS
# ============================================================================
# Step 1: Go to https://developers.facebook.com/
# Step 2: Click "My Apps" > "Create App"
# Step 3: Choose app type: "Business" or "Other"
# Step 4: Fill in app details (name, contact email)
# Step 5: Once created, go to App Settings > Basic
# Step 6: Copy "App ID" and "App Secret" (click "Show" to reveal secret)
# Step 7: Add "Marketing API" product to your app if not already added
# Step 8: Configure OAuth redirect URIs if needed for authentication flow
# ============================================================================

# ============================================================================
# AUTHENTICATION FLOW AFTER DEPLOYMENT
# ============================================================================
# After deploying to Railway, users will authenticate by:
# 1. Calling the MCP tool "get_login_link"
# 2. Following the OAuth link to grant permissions
# 3. The access token will be managed by the server
# OR
# 4. Passing X-META-ACCESS-TOKEN header directly in MCP requests
# ============================================================================
