[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'python -m meta_ads_mcp --transport streamable-http --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
# These will be set in Railway dashboard
# META_APP_ID =
# META_APP_SECRET =
# PIPEBOARD_API_TOKEN = (optional)
